import { Typography } from '@material-ui/core';

export default function Dashboard() {
  return (
    <div>
      <Typography variant="h3">
          So here I think will be MC connetions info
      </Typography>
    </div>
  )
}